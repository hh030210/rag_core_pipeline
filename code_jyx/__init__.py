"""Dimension-index v2 implementation package.

The v2 modules in this package are the single implementation source for the
hierarchical dimension schema, multi-label tags, inverted index and query
matching.  Root-level production entry points import these modules as
adapters; they must not duplicate v2 business logic.
"""

SCHEMA_VERSION = "2.0"

